import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.simon.supachat',
  appName: 'supaAuth',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
